---
idea: bmad-orchestrator: BMad-модуль мультипроектной оркестрации (DAG подпроектов, изоляция контекста через контракты, bmad-build по узлам) + многопользовательский режим
goal: проверить на прочность + улучшить
status: complete
updated: 2026-09-25T08:18
---

- (crack) Спецификация опирается на несуществующее: bmad.module.json, YAML-workflow DSL с loop/run-script, agents/*.md, CLI bmad-builder validate, bmad-loop. Реальный BMad: module.yaml+module-help.csv+setup-скилл, workflow = SKILL.md + markdown-шаги, исполнитель — LLM
- (assumption) Предполагается, что есть рантайм, детерминированно обходящий DAG и ограничивающий запись allowed_write — в BMad его нет
- (lock) Гарантии границ (D): основа — детерминированный post-hoc контроль в git (ветка/worktree на узел DAG, uv-скрипт проверяет diff против allowed_write и контракты перед merge); hooks клиента (PreToolUse) — опциональное усиление там, где доступны. Скилл только направляет.
- (crack) Изоляция чтения (allowed_read) не проверяется post-hoc: чтение не оставляет diff
- (lock) Изоляция чтения — оптимизация контекста (токены/фокус), не безопасность. Реализация: контракты соседей в контексте + инструкция + опционально hooks. Физическая изоляция (sparse checkout) не в скоупе ядра.
- (lock) Contract-first, две фазы: (1) последовательная фаза контрактов под надзором человека — фича декомпозируется в дельты контрактов, коммитятся одним коммитом; (2) параллельная реализация по узлам против зафиксированных контрактов. DAG — только для определения затронутых узлов и порядка merge/интеграционных проверок. Ошибка контракта → возврат в фазу 1, узлы не договариваются напрямую.
- (note) AI-DLC (awslabs): contract-first подтверждён (stage 2.8 contract-summary.md после DAG юнитов); swarm = git worktree на юнит + referee перезапускает check-команду, source мёржится отдельно; команды клеймят юниты через git refs refs/heads/claim/<id>/<unit> (atomic update-ref); audit-лог шардирован по клонам (audit/<host>-<clone>.md) — нет merge-конфликтов на append; state-файл общий, Unit Progress пересобирается из receipts. Дыры: нет человеческих ролей/прав (на roadmap), team ownership только single-repo, монорепо-юниты не поддержаны.
- (lock) Ролей в модуле нет. Авторизация делегирована git-хостингу (CODEOWNERS + branch protection на contracts/planning). Консистентность: claims через git refs, pinning хешей контракта/PRD в историях и ветках узлов с отказом merge при расхождении, шардированное состояние (каждый клон пишет свой файл).
- (lock) Параллельные фазы контракта двух фич — оптимистично: без блокировки, завершение фазы контракта = rebase на свежий контракт + повторное согласование.
- (lock) Не форкаем штатные скиллы BMad; интеграция только через _bmad/custom/*.toml (activation_steps_prepend, persistent_facts, on_complete). Общие файлы состояния BMad (sprint-status.yaml) — производные от шардов receipts: пересборка custom merge driver'ом (.gitattributes) + pre-merge gate отклоняет расхождение (страховка, если драйвер не зарегистрирован в клоне).
- (crack) bmad-build sync-sprint-status пишет единый sprint-status.yaml с last_updated → гарантированный конфликт при каждом merge параллельных веток
- (decision) v1 поддерживает и монорепо, и полирепо (вариант c), вопреки рекомендации 'сначала монорепо'.
- (direction) Чтобы (c) не удваивал работу: единая абстракция 'координирующий репо + code-репо'; монорепо = вырожденный случай, где это один и тот же репо.
- (lock) Координирующий репо + code-репо как единая абстракция (монорепо — вырожденный случай). Канонические контракты живут в координирующем репо (contracts/<subproject>/...), сервисы получают копии/генерацию, pre-merge gate проверяет соответствие. Цена принята: команда сервиса не меняет API единолично — только через фазу контракта + CODEOWNERS.
- (crack) Исходный манифест: exports.path внутри сервиса — противоречит contract-first и атомарности в полирепо
- (lock) Строим (не переходим на AI-DLC). Ниша: мультипроектная оркестрация для команд, уже живущих в BMad (PRD/architecture/epics/bmad-build). Полирепо-команды, пакетные узлы в монорепо и проверяемые канонические контракты — временное преимущество, не ров.
- (assumption) Первый пользователь уже работает в BMad — не подтверждено, пилотный проект не назван
- (decision) Каждая story покрывает ровно один подпроект; сквозная задача декомпозируется на stories по подпроектам (пользователь отверг модель 'сквозная story + срезы').
- (lock) Эпик = сквозная ценность; story = ровно один подпроект (тег subproject:); если эпик меняет контракт — первая story в псевдо-подпроекте contracts (координирующий репо). Настройка планирования только через _bmad/custom/bmad-create-epics-and-stories.toml persistent_facts; pre-merge gate проверяет diff против allowed_write подпроекта story.
- (crack) Внутри эпика бывают обязательные зависимости помимо контракта (миграция данных до сервиса) — модель 'все после контракта параллельны' неполна
- (lock) Выполнение планирует DAG stories (явное depends_on в story, только назад — циклов нет по штатному правилу), не DAG подпроектов. DAG подпроектов из манифестов (imports) — только для валидации плана. Зависимость выполнена = story смёржена в main (без stacked branches); простой на ревью лечится маленькими stories.
- (kill) Stacked branches / 'готово в ветке' как критерий зависимости — отвергнуто: каскадные rebase = тот же merge hell
- (lock) Состав v1: 4 скилла (setup, next, gate, status) + детерминированные uv-скрипты, без агентов. Модуль строго по официальному видению BMad Builder.
- (crack) По докам BMad Builder префикс bmad- зарезервирован за официальными модулями — имена bmad-orch-* и @bmad/orchestrator недопустимы для кастомного модуля. Дистрибуция: .claude-plugin/marketplace.json + npx bmad-method install --custom-source (не npx skills add). Мультискилл-модуль: setup-скилл {code}-setup генерируется Create Module с assets/module.yaml + module-help.csv; скрипты — в scripts/ каждого скилла, uv run с PEP 723.
- (lock) Код модуля: orch. Скиллы: orch-setup (сгенерированный Create Module + наши шаги: merge driver, _bmad/custom/*.toml, проверка манифестов), orch-next, orch-gate, orch-status. Путь сборки: IM → BW на каждый скилл → CM → VM. Дистрибуция: marketplace.json + npx bmad-method install --custom-source. Контрольная точка: не более ~4 overrides на штатный скилл, иначе форк.
- (lock) Реестр подпроектов: координирующий репо orch/subprojects/<name>.yaml (файл на подпроект, YAML); поля name, repo ('.' для монорепо или git-URL), path, allowed_read (сохраняется), allowed_write, contracts.exports[{type, canonical, copy}], contracts.imports. Без $schema на bmad-домене, без workspace.driver.
