---
topic: orch-gate: deterministic pre-merge gate + shared orch library
goal: PR merges only if it respects its story's boundaries and contracts; same verdict locally and in CI
updated: 2026-09-25T16:34
---

- (note) context: module plan skills/reports/orch-module-plan.md (orch-gate brief, Memory Architecture, Configuration, External Dependencies) + forged decisions _bmad-output/forge/bmad-orchestrator/forged-idea.md (locked 1-16); plan is not re-litigated
- (decision) name orch-gate (module orch, expands bmm); workflow skill, script-first; built first per roadmap because it owns the shared orch library
- (note) stock epics.md has no per-story metadata (only '### Story N.M:' headings); sprint_plan.py STORY_RE/keys N-M-slug; bmad-build writes in-progress/review into sprint-status itself
- (decision by user) story metadata = bold labels under the story heading: **Subproject:**, **Depends on:** (comma list of N.M), **Contract change:** none|expand|narrow; orch-gate owns this schema, the epics override must emit it
- (decision) gate reads story->subproject from epics.md on coordination main, never from the PR's marker (marker is author-controlled, would bypass scope)
- (decision by user) pins: canonical contracts the story subproject imports/exports pinned by git blob sha; mismatch vs coordination main = FAIL; PRD pin recorded, mismatch = WARN only
- (decision by user) sprint-status check = done invariants only: story done <=> marker in main or this PR (incl. .orch/archive/); epic done only with close record; in-progress/review free (stock bmad-build writes them)
- (decision by user) shared library exposed as one CLI scripts/orch.py with JSON subcommands (gate, registry, stories, claim, take-over, sprint-status derive/check, merge-driver); other orch skills call it via uv run, no cross-skill imports
- (assumption) marker/claim id = N-M (e.g. 1-2), not the slugged sprint key, so renames don't break refs; polyrepo: gate takes --coord PATH, CI template checks out coordination repo; narrow story must depends_on a merged story for every registry consumer of the exporter; epic close record file in coordination repo; archive at .orch/archive/epic-N/; dependency pyyaml via PEP 723
- (decision by user) no customize.toml: verdict must be identical in CI and locally; settings come from module config orch_* with plan defaults as fallback
- (decision by user) external dependency pyyaml>=6 via PEP 723 approved
- (decision) contract stories pin what they produce (canonical at PR head) and must branch from the latest contract (merge-base blob == main blob); implementation stories pin what they build against (coordination main). This makes pins converge after merge, so the epic close check can require convergence
- (decision) detector missing = FAIL only when a contract of that type changes in the PR (fail closed, no silent skip); registry-wide availability reported by 'orch.py deps' for setup. Deviation from plan wording 'present in registry = fail' to avoid forcing detectors into every code-repo CI
- (decision) sprint-status checked only when a coordination PR touches sprint-status.yaml or the closed/ dir; forward invariant (done without marker, epic done without close record) FAILS, reverse lag (merged but not done) only WARNS because polyrepo merges never touch the coordination file; derive --write fixes lag
- (decision) gate enforces the epic close check itself: a PR adding closed/epic-N.yaml fails unless all epic stories merged, markers archived under .orch/archive/epic-N/, pins converged. Same function exposed as 'orch.py epic close-check' for orch-status
- (decision) non-story PRs (no marker) allowed only outside every subproject's allowed_write in this repo and outside the contracts dir; marker removal allowed only as an unchanged move to .orch/archive/epic-N/
- (assumption) coordination-repo stories may also write {implementation_artifacts}/** (stock bmad-build writes spec files and sprint-status there)
- (decision) merge-driver check runs only in local (non-CI) runs: fails when .gitattributes declares the driver but this clone has not registered it
- (gap) asyncapi and atlas adapter command lines are unverified against real binaries (not installed here); oasdiff/buf follow documented usage. Verify during the pilot. Conformance is byte copy only; generated-code conformance is post-v1
- (event) library + CLI built: scripts/orch.py + orchlib (config, gitio, globs, registry, stories, markers, claims, sprint_status, detectors, gate); 32 pytest cases incl. monorepo, polyrepo via fetch cache, remote claim race, real git merge through the driver — all pass
- (event) SKILL.md written (1557 tokens, under desired 2000); lint gate: quick_validate ok, scan-scripts pass, scan-path-standards flags only .memlog.md at root (builder's own memlog location, kept); handed off, full Analyze offered
- (event) analyze: grade poor, 2 critical / 7 high / 14 medium / 10 low, report .analysis/2026-09-25-1158/skill-analysis-report.html
- (decision) fix determinism-1 (critical): gate.run resolves head via gitio.resolve_head; a merge commit whose first parent is on base and second is not (GitHub refs/pull/N/merge, GitLab merged results) is gated as the second parent; result carries head_sha and head_resolved. Test: branch tip and merge ref give identical pins findings
- (decision) fix customization-1/-2 (critical/high): config.load reads _bmad layers through gitio.Tree at a trusted ref, never the working tree; config.resolve bootstraps from origin/HEAD, else origin/main or main, then follows orch_main_branch one hop; monorepo gate reads config/registry/epics at --base. Gitignored *.user.toml drop out because they are never committed. Tests: PR editing _bmad/custom/config.toml plus a local user layer cannot move boundaries; merged config change takes effect
- (event) critical fixes applied: 35 pytest pass; both new tests fail with the fixes reverted; SKILL.md gotchas updated
- (decision) high fixes: gate result records base_sha, head_sha, coord_ref, coord_sha, offline and notices; local runs add an informational dirty-worktree notice (ignores .orch/cache) that never changes the verdict (determinism-3, enhancement-3)
- (decision) high fixes: merge_base names the shallow-clone fix (fetch-depth 0) instead of an empty git error (enhancement-2)
- (decision) high fixes: markers.merged returns structured unread repos (repo-offline / repo-unreadable, narrow except); stories in unread repos are unknown, reported as done-unverifiable / consumers-unverifiable / close-check unverifiable, still failing closed; derive refuses to run with unread repos; --offline refused in CI; epic close-check exits 1 only on problems (determinism-4, architecture-4)
- (decision) high fixes: coordination repo locator = --coord > ORCH_COORD > local path in this repo's committed orch_coordination_repo (relative to repo root) > this repo; a URL naming this repo's origin means this repo; any other non-local value exits 2 asking for --coord (customization-3, determinism-11)
- (decision) high fixes: SKILL.md explains the CI JSON/log when the user has it and compares base_sha/head_sha/coord_sha/notices/unread_repos when local and CI differ (enhancement-1)
- (event) high fixes applied on branch fix/orch-gate-high: 41 pytest pass (6 new), scan-scripts pass
- (note) PR #5 (high fixes) was closed automatically when its stacked base feat/orch-gate was deleted after #4 merged; re-opened as #6 against main after rebasing fix/orch-gate-high. Stacked PRs in this repo lose their base on merge
- (decision) by user: registry-only (gate-only, no BMad planning) adoption is post-v1; recorded in the plan with an orch_story_mode opt-in sketch and as a SKILL.md gotcha (enhancement-8)
- (decision) medium fixes: every gate finding has a stable code; mechanical ones carry fix {mechanical, command, precondition}; SKILL.md offers fix.command, then commit, re-run and report instead of mapping free-text hints (determinism-7, enhancement-4)
- (decision) medium fixes: conformance copy-drift carries a unified diff (60-line cap); gate --format markdown for CI step summaries; unexpected exceptions exit 2 with JSON (traceback under --verbose), non-UTF-8 files raise OrchError; is_ci parses CI strictly (enhancement-5, enhancement-6, determinism-8, determinism-9)
- (decision) medium fixes: atlas lints a detached worktree of the head commit, not the working tree, and records dev_url_source; detector versions go into gate detectors_used and deps output, install hints ask for a pinned version; deps --probe runs installed openapi/protobuf/asyncapi adapters on built-in compatible and breaking fixtures, atlas unprobed (determinism-5, determinism-6, enhancement-7)
- (decision) medium fixes: other orch skills call <calling skill dir>/../orch-gate/scripts/orch.py (skills install side by side); SKILL.md library table collapsed to one paragraph; communication_language resolved from _bmad config; orch_main_branch added to the plan config table (architecture-1, architecture-2, architecture-3, leanness-1, leanness-2, customization-4)
- (event) medium fixes applied on branch fix/orch-gate-medium (stacked on fix/orch-gate-high / PR #6): 47 pytest pass (6 new), SKILL.md 1703 tokens, quick_validate and scan-scripts pass
- (event) analyze (re-run after critical/high/medium fixes): grade fair, 0 critical / 3 high / 10 medium / 10 low, report .analysis/2026-09-25-1326/skill-analysis-report.html
- (note) correction: 2026-09-25-1326 analyze tally is 0 critical / 3 high / 10 medium / 11 low (24 findings)
- (decision) by user (walkthrough of 2026-09-25-1326, theme fail-open setup): registry.validate issues on coordination main FAIL every PR with registry-invalid; escape hatch = markerless coordination-repo PR that changes only registry files and whose head registry validates passes as a registry repair (otherwise a broken registry on main deadlocks its own fix) (determinism-2, enhancement-1)
- (decision) by user: gate FAILs (setup check, stable codes) on empty registry, no-epics, and in polyrepo on repo-unregistered (repo_id matches no subproject.repo); each names the fix (orch-setup, --coord, --repo-id); missing required registry fields never default permissively (repo no longer defaults to '.') (enhancement-1)
- (decision) by user: allowed_write patterns without wildcard or trailing '/' follow gitignore semantics (match the path and everything below it); no-story protection stays allowed_write only, not sub.path/** (determinism-6)
- (decision) by user (theme parsing primitives): Tree.read returns blobs only; directory canonicals allowed for protobuf (whole subtree materialized for buf breaking) and db-schema; openapi/asyncapi canonicals must be a single file, a directory is a registry-invalid issue (multi-file $ref specs post-v1); conformance compares directory copies file by file (missing/extra/changed, per-file diff) (determinism-1)
- (decision) by user: narrow-story consumer check maps each consumer to the set of its dependency stories; the consumer counts as migrated only when all of them are merged, independent of Depends on order (determinism-4)
- (decision) sprint-status check takes status semantics from yaml.safe_load development_status; line parsing stays only for comment-preserving writes (derive, merge driver); disagreement between the two FAILs. Archive checks compare the epic number in .orch/archive/epic-N/ with the story's epic (determinism-3, determinism-11)
- (decision) by user (theme fix.command): fix.command is a full runnable argv list: uv run <orch.py> <subcommand args> plus the gate's explicitly passed context flags (--coord, --coord-ref, --repo, --repo-id, --base); orch.py and --coord paths are written relative to the gated repo's root when inside or beside it, else absolute; run from the repo root. SKILL.md runs gate --format text -o <tmp>.json and reasons about fix.mechanical and the CI comparison from the JSON (enhancement-2, architecture-2, determinism-8, architecture-1)
- (decision) by user (theme verdict inputs): gate result adds repos_read [{repo, branch, sha}] and SKILL.md's local-vs-CI comparison includes it; default base in a code repo = registry branch of the subproject(s) whose repo matches (conflicting branches exit 2 asking for --base), recorded as base_source; result records coord_ref_source and the bootstrap error names --coord-ref; empty project_name derives from the origin URL repo name before the directory name (determinism-5, determinism-7, determinism-10)
- (decision) by user: no gate-diff/--compare subcommand; comparing a handful of scalar input fields between two results stays in the prompt, since a subcommand would add code and tests to replace one sentence (determinism-9 rejected)
- (decision) by user (theme config/SKILL.md drift): verdict config layers are only _bmad/config.toml and _bmad/custom/config.toml read at the trusted ref (a tracked _bmad/config.user.toml no longer reaches the verdict); user-setting keys (currently orch_worktrees_dir) resolve separately from the working tree across all four layers incl. *.user.toml, are exposed by orch.py config, and never feed gate.run (customization-1, customization-2)
- (decision) by user: SKILL.md 'Library for other orch skills' becomes a short 'CLI behavior' section (exit codes, --format markdown, deps --probe) per the leanness proposed_smallest, without a variant eval; the sibling-skill calling convention moves to the orch.py module docstring (--help) and the module plan. Also: {communication_language} from custom user > user > custom team > team; define {planning_artifacts}/{implementation_artifacts} as orch.py config values; registry-read sentence says base in monorepo, coordination main otherwise; drop the must-emit directive aimed at the epics override (leanness-1, leanness-2, architecture-3, architecture-4, architecture-5, customization-3)
- (decision) by user: optional pre-push hook running gate --format text becomes an orch-setup Setup Extension in the module plan; orch-gate itself does not change (enhancement-3)
- (decision) setup check refinements found while implementing: registry issues that only say something does not exist yet (path-missing, canonical-missing) warn instead of failing, because a new subproject's first story or a contract story adding its canonical would otherwise deadlock; the setup-repair escape covers changes confined to registry and planning files (not registry only), so a missing-epics main can also be repaired; the repair is judged by re-running the setup check on the PR head
- (gap) buf breaking on a materialized protobuf package directory (no buf.yaml) is exercised only with a fake buf; verify against the real binary during the pilot, alongside the asyncapi and atlas gaps
- (event) rerun fixes applied on branch fix/orch-gate-rerun-findings: all 24 findings of 2026-09-25-1326 addressed per the walkthrough decisions (determinism-9 rejected by design); 62 pytest pass (15 new, all failing with the fixes reverted); also fixed dirty_paths dropping the first path character (found in a smoke run); SKILL.md 1915 tokens; quick_validate, scan-scripts pass, path-standards clean for SKILL.md; plan updated (calling convention, pre-push setup extension, bootstrap order, epics override must emit story metadata)
- (event) analyze (third run, after rerun fixes): grade fair, 0 critical / 2 high / 9 medium / 5 low, report .analysis/2026-09-25-1532/skill-analysis-report.html; all 24 findings of 2026-09-25-1326 verified fixed
- (decision) by user (walkthrough of 2026-09-25-1532, theme machine-dependent verdict): every git call pins output-affecting config (-c core.quotePath=false) and tree listings use ls-tree -z, so the same commit gives the same verdict on every machine; regression test with a non-ASCII registry file and epics subdirectory under both quotePath settings (determinism-1)
- (decision) by user: local runs (not CI, not --offline) fetch the base branch and the coordination ref before reading them and record refs_fetched; a failed fetch adds a refs-not-refreshed notice naming the ref and never changes the verdict (enhancement-1)
- (decision) by user: CI is detected from CI plus the common provider variables (GITHUB_ACTIONS, GITLAB_CI, JENKINS_URL, TF_BUILD, TEAMCITY_VERSION, BUILDKITE) or --ci, recorded as ci_source in the result; orch-setup CI templates still pass --ci (determinism-7)
- (decision) by user: render_text and render_markdown print one compact inputs line (base@sha and source, head sha, coord ref@sha and source, repos_read, detectors_used); the orch-setup CI templates upload the -o JSON as a job artifact (enhancement-3)
- (decision) by user (theme registry and archive fail-open): .orch/ is protected in every mode; the only allowed changes are adding the PR's own marker, an unchanged move of a marker into its own epic's archive, and adding closed/epic-N.yaml; any modify or delete under .orch/archive or .orch/closed FAILs archived-marker-modified / archived-marker-removed (determinism-2)
- (decision) registry.load is total: wrong shapes (non-mapping contracts, non-list exports or allowed_write, non-string scalars) and empty required values ([] or blank repo/path/allowed_write) become registry-invalid issues with the entry dropped, never exceptions or permissive defaults, so setup-repair can always run; write-overlap compares normalize_repo values (determinism-3, determinism-4, determinism-5)
- (decision) by user: story-plan issues surface in the gate: issues naming the PR's own story (duplicate, forward or unknown dependency, missing or unknown subproject, contract change outside contracts) FAIL under marker with stable codes; issues of other stories warn under setup, so a typo in a future epic never blocks unrelated PRs (architecture-2)
- (decision) by user (theme derive vs gate merged set): the reverse-lag merged-not-done warning skips the PR's own story (not merged until the PR merges); the forward invariant keeps using markers at head, so a PR may mark its own story done; for every other story the gate and derive already agree; the standalone epic close-check stays a preflight on coordination main by design; test that applying the gate's fix.command clears its finding (architecture-1, determinism-6)
- (decision) by user (theme prompt residue): communication_language joins config.USER_KEYS and resolves in stock BMad order (custom user > custom team > user > team, replacing the earlier custom user > user > custom team > team order); SKILL.md takes it from orch.py config like the artifact paths (customization-1, determinism-8)
- (decision) by user: SKILL.md Defaults paragraph replaced by the leanness proposed_smallest (script resolves and records base/coord/coord-ref sources, failures name the flag, registry read at base or coordination main so a PR cannot move its own boundaries), without a variant eval (leanness-1)
- (decision) by user: SKILL.md tells the agent to gate a named PR or branch that is not the current checkout with --head and --base; an empty change set adds an empty-diff notice and keeps the PASS verdict so CI reruns and commitless pre-push stay green (enhancement-2)
- (decision) by user: the registry-repo fetch cache moves under git rev-parse --git-common-dir (orch-cache) so it can never be committed and is shared across worktrees; the .orch/cache exceptions and the orch-setup gitignore step go away (architecture-3)
- (note) correction to the archive decision: epic close records live in the orch closed dir beside the registry (<orch dir>/closed/epic-N.yaml), not under .orch/; the gate treats both .orch/archive and that closed dir as append-only, and anything else under .orch/ other than story markers fails orch-record-changed
- (note) local refresh fetches the whole origin remote (git fetch --no-tags origin, GIT_TERMINAL_PROMPT=0) in this repo and, in a polyrepo, the coordination clone, before config and refs are resolved; an explicit --base naming a local branch (e.g. main) is not updated by it, only origin/* refs are
- (event) third-run fixes applied on branch fix/orch-gate-rerun-findings (PR #8): all 16 findings of 2026-09-25-1532 addressed per the walkthrough; 74 pytest pass (13 new or extended, all failing on the previous commit); smoke run on this repo shows the inputs line and a real origin fetch; SKILL.md 1968 tokens; quick_validate, scan-scripts pass, path-standards clean for SKILL.md; plan updated (cache under git dir, CI templates upload the JSON artifact, gitignore hygiene step dropped)
- (decision by user) a PR may only add a story marker main has no marker for (live or archived); modifying a merged marker or re-adding an archived one fails marker-already-merged, since a merged story is never reopened (PR #8 review)
- (decision by user) a coordination PR that touches registry or planning files is also judged by the setup its head produces: a setup failure absent on main fails setup as 'introduced by this PR'; setup-repair still applies when the head resolves all of main's problems (PR #8 review)
- (event) PR #8 code review fixes: git calls drop hook repo variables (GIT_DIR and friends), the shared fetch cache is locked per repo and clears stale git locks, git output decodes with surrogateescape, submodules compare by commit and fail closed in detectors, local fetch times out after 60s, registry repo './' canonicalizes to '.', blank or non-string branch/copy/dev_url/path and deep YAML are issues, unreadable epics fail setup, fixes for a non-checked-out --head say to check it out first, duplicate archive and canonical-missing findings removed; 85 pytest pass, the 11 new ones all fail on 33e4aa8
