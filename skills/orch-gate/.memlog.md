---
topic: orch-gate: deterministic pre-merge gate + shared orch library
goal: PR merges only if it respects its story's boundaries and contracts; same verdict locally and in CI
updated: 2026-09-25T12:09
---

- (note) context: module plan skills/reports/orch-module-plan.md (orch-gate brief, Memory Architecture, Configuration, External Dependencies) + forged decisions _bmad-output/forge/bmad-orchestrator/forged-idea.md (locked 1-16); plan is not re-litigated
- (decision) name orch-gate (module orch, expands bmm); workflow skill, script-first; built first per roadmap because it owns the shared orch library
- (note) stock epics.md has no per-story metadata (only '### Story N.M:' headings); sprint_plan.py STORY_RE/keys N-M-slug; bmad-build writes in-progress/review into sprint-status itself
- (decision by user) story metadata = bold labels under the story heading: **Subproject:**, **Depends on:** (comma list of N.M), **Contract change:** none|expand|narrow; orch-gate owns this schema, the epics override must emit it
- (decision) gate reads story->subproject from epics.md on coordination main, never from the PR's marker (marker is author-controlled, would bypass scope)
- (decision by user) pins: canonical contracts the story subproject imports/exports pinned by git blob sha; mismatch vs coordination main = FAIL; PRD pin recorded, mismatch = WARN only
- (decision by user) sprint-status check = done invariants only: story done <=> marker in main or this PR (incl. .orch/archive/); epic done only with close record; in-progress/review free (stock bmad-build writes them)
- (decision by user) shared library exposed as one CLI scripts/orch.py with JSON subcommands (gate, registry, stories, claim, take-over, sprint-status derive/check, merge-driver); other orch skills call it via uv run, no cross-skill imports
- (assumption) marker/claim id = N-M (e.g. 1-2), not the slugged sprint key, so renames don't break refs; polyrepo: gate takes --coord PATH, CI template checks out coordination repo; narrow story must depends_on a merged story for every registry consumer of the exporter; epic close record file in coordination repo; archive at .orch/archive/epic-N/; dependency pyyaml via PEP 723
- (decision by user) no customize.toml: verdict must be identical in CI and locally; settings come from module config orch_* with plan defaults as fallback
- (decision by user) external dependency pyyaml>=6 via PEP 723 approved
- (decision) contract stories pin what they produce (canonical at PR head) and must branch from the latest contract (merge-base blob == main blob); implementation stories pin what they build against (coordination main). This makes pins converge after merge, so the epic close check can require convergence
- (decision) detector missing = FAIL only when a contract of that type changes in the PR (fail closed, no silent skip); registry-wide availability reported by 'orch.py deps' for setup. Deviation from plan wording 'present in registry = fail' to avoid forcing detectors into every code-repo CI
- (decision) sprint-status checked only when a coordination PR touches sprint-status.yaml or the closed/ dir; forward invariant (done without marker, epic done without close record) FAILS, reverse lag (merged but not done) only WARNS because polyrepo merges never touch the coordination file; derive --write fixes lag
- (decision) gate enforces the epic close check itself: a PR adding closed/epic-N.yaml fails unless all epic stories merged, markers archived under .orch/archive/epic-N/, pins converged. Same function exposed as 'orch.py epic close-check' for orch-status
- (decision) non-story PRs (no marker) allowed only outside every subproject's allowed_write in this repo and outside the contracts dir; marker removal allowed only as an unchanged move to .orch/archive/epic-N/
- (assumption) coordination-repo stories may also write {implementation_artifacts}/** (stock bmad-build writes spec files and sprint-status there)
- (decision) merge-driver check runs only in local (non-CI) runs: fails when .gitattributes declares the driver but this clone has not registered it
- (gap) asyncapi and atlas adapter command lines are unverified against real binaries (not installed here); oasdiff/buf follow documented usage. Verify during the pilot. Conformance is byte copy only; generated-code conformance is post-v1
- (event) library + CLI built: scripts/orch.py + orchlib (config, gitio, globs, registry, stories, markers, claims, sprint_status, detectors, gate); 32 pytest cases incl. monorepo, polyrepo via fetch cache, remote claim race, real git merge through the driver — all pass
- (event) SKILL.md written (1557 tokens, under desired 2000); lint gate: quick_validate ok, scan-scripts pass, scan-path-standards flags only .memlog.md at root (builder's own memlog location, kept); handed off, full Analyze offered
- (event) analyze: grade poor, 2 critical / 7 high / 14 medium / 10 low, report .analysis/2026-09-25-1158/skill-analysis-report.html
- (decision) fix determinism-1 (critical): gate.run resolves head via gitio.resolve_head; a merge commit whose first parent is on base and second is not (GitHub refs/pull/N/merge, GitLab merged results) is gated as the second parent; result carries head_sha and head_resolved. Test: branch tip and merge ref give identical pins findings
- (decision) fix customization-1/-2 (critical/high): config.load reads _bmad layers through gitio.Tree at a trusted ref, never the working tree; config.resolve bootstraps from origin/HEAD, else origin/main or main, then follows orch_main_branch one hop; monorepo gate reads config/registry/epics at --base. Gitignored *.user.toml drop out because they are never committed. Tests: PR editing _bmad/custom/config.toml plus a local user layer cannot move boundaries; merged config change takes effect
- (event) critical fixes applied: 35 pytest pass; both new tests fail with the fixes reverted; SKILL.md gotchas updated
