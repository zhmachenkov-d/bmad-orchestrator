---
topic: orch-status: epic progress, anomalies, take over, close epic, plan validation, report
goal: Anyone sees where every epic stands across subprojects, what is blocked and why, and acts on it; epic becomes done only through the close check
updated: 2026-09-26T06:58
---

- (note) context: module plan skills/reports/orch-module-plan.md (orch-status brief, Memory Architecture, Configuration, UI) + skills/orch-gate/.memlog.md (sibling build log, lessons reused); plan is not re-litigated
- (decision) name orch-status (module orch, expands bmm); workflow skill, script-first; calls the shared library via uv run <this skill dir>/../orch-gate/scripts/orch.py, never imports orchlib or duplicates it
- (note) orch.py already provides: config, registry, stories (plan issues), merged (unread_repos), claim list/take-over/release (--expect lease), sprint-status check/derive/merge/install-driver, epic close-check. Missing for orch-status: aggregated status view, anomalies, close-epic action (archive markers + closed/epic-N.yaml), epic report
- (decision) by user: status view + anomalies live in the shared library as 'orch.py status' in orch-gate (orch-next reuses it for its warnings); close-epic and report builders also go into the library; orch-status SKILL.md stays a thin renderer and decision layer
- (decision) by user: close epic = two passes through PRs, never direct commits to main. Pass 1: per repo, branch orch/close-epic-N with the unchanged marker moves to .orch/archive/epic-N/; skill pushes and opens PRs via gh/glab when present, else prints branch and compare hint. Pass 2 (after archive PRs merged): coordination PR adding closed/epic-N.yaml and epic-N: done; the gate's close check verifies each step
- (decision) by user: story branch convention story/<N-M> in the story subproject's repo (orch-next must use the same name); last activity = max(claim commit time, remote story branch tip commit time); no branch = claim time only; stale when older than orch_stale_claim_hours
- (decision) by user: epic report is always a self-contained HTML with print CSS; PDF via headless Chromium/Chrome --print-to-pdf when found, else offer printing from the browser; no heavy Python deps (no weasyprint)
- (decision) by user: no customize.toml; thresholds and paths come from module config orch_* via orch.py config (same as orch-gate)
- (decision) by user: plan validation = 'orch.py plan-check', deterministic verdict with stable codes. FAIL: structural story issues from orch.py stories, and a narrow story that does not depend on a story of every registry consumer of the exporter. CONCERNS: cross-subproject dependency not backed by registry imports (contracts excepted); narrow without an earlier expand story. bmad-sprint-planning override calls it
- (decision) by user: contract bottleneck = unmerged contracts story with >=2 dependents waiting only on it (all their other deps merged), or on the epic's critical path; stories ranked by transitive dependent count
- (decision) by user extras: (1) close epic writes a retro data file orch-epic-N.json (review waits, take-overs, bottlenecks, critical path) beside the report for bmad-retrospective; (2) release claim offered besides take over (claim release exists); (3) pin drift offers a drafted migration story (Subproject/Depends on/Contract change metadata) for the user to add to the epics
- (assumption) review wait: PR age from gh/glab for head story/<N-M> when available; without them degrade to age of the pushed story branch since its first commit off main (plan: degrade to branch age). Pin drift = a merged marker whose contract_pins differ from coordination main
- (decision) by user (gap found in orch-gate close_check: pins freeze at merge, so any later contract change in the epic, e.g. expand->migrate->narrow, made close impossible forever): pin converged = equals coordination main, or every later version of that canonical produced by a merged contract story (chain ordered by story order, from contract-story markers) is 'expand', or 'narrow' that depends on a story of the pinning subproject; contracts-story pins are converged when they sit in the chain. One function in orch-gate used by epic close-check (and the gate) and by the pin-drift anomaly; fixed in orch-gate on this branch with tests
- (decision) refinement of the convergence rule found while designing tests: only the latest merged story (plan order) of a subproject that pins a canonical speaks for it; earlier markers are superseded. Otherwise the exporter's pre-narrow marker would strand close forever even after the exporter's post-narrow story. Implemented as markers.PinIndex (chain + latest pinner) used by close_check and the pin-drift anomaly
- (event) library built in orch-gate: orchlib status.py (states, critical path, anomalies stale-claim/stuck-review/contract-bottleneck/pin-drift/claim-on-merged/merge-driver-missing/sprint-status-lag with orch.py action args), plan.py, close.py (plumbing commits via update-index --index-info so the bare fetch cache works, push new branch or report exists), report.py (HTML with SVG DAG, PDF via Chromium); orch.py status, plan-check, epic close [--push], report; status/report/epic refresh origin on local runs like the gate
- (event) tests: 98 pass (13 new in tests/test_status.py incl. monorepo expand->migrate->narrow close through two gate-checked passes and polyrepo push from the shallow cache); the two convergence tests fail under the old strict pin rule
- (event) SKILL.md written (1426 tokens, under desired 2000); quick_validate ok, scan-scripts clean, path-standards flags only .memlog.md at root (builder memlog location, kept as in orch-gate); module plan updated (story/<N-M> branch for orch-next, library commands, PDF via Chromium, close passes, convergence rule); orch-gate SKILL.md exit-code line updated
- (gap) gh/glab command lines (gh pr list --json headRefName,createdAt,url; glab mr list --output json) tested only with a fake gh; verify against real hosts in the pilot. Retro data covers claim history only for claims still present and review ages only at close time
